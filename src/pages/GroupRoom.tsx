
import { Layout } from "@/components/Layout";
import { EnhancedGroupRoom } from "@/components/gpo/EnhancedGroupRoom";

const GroupRoom = () => {
  return (
    <Layout>
      <EnhancedGroupRoom />
    </Layout>
  );
};

export default GroupRoom;
